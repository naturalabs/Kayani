{{ 'section-main-page.css' | asset_url | stylesheet_tag }}

{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
{%- endstyle -%}

<div class="collection">
  <div class="collection-header">
    <h1>{{ section.settings.collections_header }}</h1>
  </div>
  <div class="collection-container">
    <div class="collection-card">
      <h2>{{ section.settings.bestseller_title | upcase }}</h2>
      <a href="/collections/{{section.settings.bestseller_link}}">VIEW ALL</a>

      <div class="collection-card__image-container">
        <div class="collection-card__image">
          {%- if section.settings.bestsellers_image != blank -%}
            <img
              src="{{ section.settings.bestsellers_image | image_url }}"
              width="auto"
              height="auto"
              alt=""
              loading="lazy"
            >
          {%- else -%}
            <img
              src="{{ 'collection-bestseller.png' | asset_url }}"
              width="auto"
              height="auto"
              alt="example"
              loading="lazy"
            >
          {%- endif -%}
        </div>
      </div>
    </div>
    <div class="collection-card">
      <h2>{{ section.settings.new_arrivals_title | upcase }}</h2>
      <a href="/collections/{{section.settings.new_arrivals_link}}">VIEW ALL</a>

      <div class="collection-card__image-container">
        <div class="collection-card__image">
          {%- if section.settings.new_arrivals_image != blank -%}
            <img
              src="{{ section.settings.new_arrivals_image | image_url }}"
              width="auto"
              height="auto"
              alt=""
              loading="lazy"
            >
          {%- else -%}
            <img
              src="{{ 'collection-new-arrivals.png' | asset_url }}"
              width="auto"
              height="auto"
              alt="example"
              loading="lazy"
            >
          {%- endif -%}
        </div>
      </div>
    </div>
    <div class="collection-card">
      <h2>{{ section.settings.dresses_title | upcase }}</h2>
      <a href="/collections/{{section.settings.dresses_link}}">VIEW ALL</a>

      <div class="collection-card__image-container">
        <div class="collection-card__image">
          {%- if section.settings.dresses_image != blank -%}
            <img
              src="{{ section.settings.dresses_image | image_url }}"
              width="auto"
              height="auto"
              alt=""
              loading="lazy"
            >
          {%- else -%}
            <img
              src="{{ 'collection-dresses.png' | asset_url }}"
              width="auto"
              height="auto"
              alt="example"
              loading="lazy"
            >
          {%- endif -%}
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  .collection h1 {
    // width: 335px;
    //  margin: 46px auto 35px;
  }
  .collection-container {
    //  display: flex;
    // align-items: center;
    // flex-direction: column;
    // margin-bottom: 50px;
    display: flex;
    flex-direction: column;
    //grid-template-columns: 1fr;
    // grid-gap: 20px;
  }
  .collection h1 {
    font-family: Cardo;
    font-size: 48px;
    font-style: normal;
    font-weight: 400;
    line-height: 60px;
    letter-spacing: -0.96px;
    text-align: center;
  }
  .collection-card {
    text-align: center;
    background-color: white;
    width: 375px;
    padding: 40px 20px;
    margin: 0 auto;
  }
  .collection-card h2 {
    font-family: Cardo;
    font-size: 28.161px;
    line-height: 35.671px;
    margin: 0;
  }
  .collection-card a {
    font-family: Unna;
    font-size: 18px;
    line-height: 25px;
    text-decoration: none;
  }
  .collection-card a:link,
  .collection-card a:visited,
  .collection-card a:active {
    color: #b2967d;
  }

  .collection-card__image-container {
    position: relative;
    height: 410px;
    width: min(95%, 335px);
    margin: 0 auto;
  }

  .collection-card__image {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    overflow: hidden;
  }
  .collection-card__image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: -1;
  }
  @media (min-width: 850px) {
    .collection {
      padding: 100px 0 107px;
    }
    .collection-container {
      //grid-template-columns: repeat(auto-fit, minmax(335px, 1fr));
      //grid-template-columns: repeat(auto-fit, minmax(335px, 1fr));
      flex-direction: row;
      max-width: 1400px;
      margin: 0 auto;
    }
    .collection-header {
      max-width: 1400px;
      margin: 0 auto;
    }
    .collection h1 {
      width: 500px;
      margin: 0;
      margin-bottom: 17px;
    }

    .collection-card {
      text-align: center;
      background-color: white;
      width: 475px;
      padding: 40px 0px;
      margin: 0 auto;
    }
    .collection-card__image-container {
      height: 490px;
      width: min(95%, 410px);
    }
  }
</style>
{% schema %}
{
  "name": "t:sections.main-page.name",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "text",
      "id": "collections_header",
      "label": "Collections Header",
      "default": "Our Unique Collections"
    },
    {
      "type": "text",
      "id": "bestseller_title",
      "label": "Best Seller Title",
      "default": "BestSellers"
    },
    {
      "type": "collection",
      "id": "bestseller_link",
      "label": "Best Seller Link"
    },
    {
      "type": "image_picker",
      "id": "bestseller_image",
      "label": "Best Seller Image"
    },
    {
      "type": "text",
      "id": "new_arrivals_title",
      "label": "New Arrivals Title",
      "default": "New Arrivals"
    },
    {
      "type": "collection",
      "id": "new_arrivals_link",
      "label": "New Arrivals Link"
    },
    {
      "type": "image_picker",
      "id": "new_arrivals_image",
      "label": "New Arrivals Image"
    },
    {
      "type": "text",
      "id": "dresses_title",
      "label": "New Arrivals Title",
      "default": "Dresses"
    },
    {
      "type": "collection",
      "id": "dresses_link",
      "label": "Dresses Link"
    },
    {
      "type": "image_picker",
      "id": "dresses_image",
      "label": "Dresses Image"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom",
      "default": 36
    }
  ]
}
{% endschema %}
